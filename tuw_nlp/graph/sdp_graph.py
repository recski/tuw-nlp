from tuw_nlp.graph.graph import Graph

from tuw_nlp.graph.graph import Graph


class SDPGraph(Graph):
    def __init__(self, graph=None, text=None, tokens=None, type=None):
        super().__init__(graph, text, tokens, type)
